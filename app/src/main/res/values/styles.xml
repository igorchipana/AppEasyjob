<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="exampletoast">
      <item name="colorBackground">#d12d2d</item>
        <item name="textColor">@color/white</item>
        <item name="iconLeft">@drawable/ic_action_nametriste</item>
        <item name="strokeColor">#860303</item>
        <item name="strokeWidth">1dp</item>
        <item name="length">SHORT</item>
    </style>

    <style name="exito_toast">
        <item name="colorBackground">#3bd12d</item>
        <item name="textColor">@color/white</item>
        <item name="iconLeft">@drawable/ic_action_nameexito</item>
        <item name="strokeColor">#117808</item>
        <item name="strokeWidth">1dp</item>
        <item name="length">SHORT</item>
    </style>

</resources>
